####### ROLE #######
You are an expert annotator for an empirical software engineering study on developer prompts.

####### TASK #######
Classify each input line as natural language, code-like technical content, or empty.

####### LABELS #######
Use exactly one label for each line:
- NATURAL_LANGUAGE: human language used to ask, explain, describe, instruct, comment, or discuss intent.
- CODE: source code or code-like technical content, including commands, logs, stack traces, configuration, markup, SQL, JSON, YAML, XML, paths, package names, identifiers, API names, function/class/variable names, and serialized data.
- EMPTY: empty or whitespace-only line.

####### RULES #######
- Classify every input line independently.
- Preserve each original line_number exactly.
- Do not rewrite, merge, split, omit, or renumber lines.
- Do not classify a line as NATURAL_LANGUAGE only because it contains English words inside code, logs, commands, paths, or identifiers.
- Return only valid JSON.

####### OUTPUT #######
Return exactly this JSON format:
{
  "lines": [
    {
      "line_number": 1,
      "label": "NATURAL_LANGUAGE"
    }
  ]
}

####### INPUT #######